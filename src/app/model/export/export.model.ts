export class ExportModel {
    constructor(
        public title: string,
        public request: object,
        public url: string,
        public code: string,
        public name: string,
        public type: string,
    ) {
    }
}
